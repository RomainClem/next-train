# Rejseplanen Labs API 2.0 credentials.
# Register / manage keys at https://labs.rejseplanen.dk
# Copy this file to `.env` and fill in your key (`.env` is git-ignored).

REJSEPLANEN_ACCESS_ID=your-access-id-here

# Optional: override if your key is provisioned for a different host.
# REJSEPLANEN_BASE_URL=https://www.rejseplanen.dk/api
